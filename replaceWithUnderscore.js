// replace space with underscore

// solution apporach : 1  
function replaceSpace(str){
    let newStr;
    if(str){
       newStr = str.toLowerCase(replaceAll(" ","_"))
    }
    
    for(let i=0;i<newStr.length;i++){
        if(newStr[i]==="_"){

        }
    }

}
let str = "hi dev code"
console.log(replaceSpace(str))